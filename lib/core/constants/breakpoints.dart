/// VitalShield AI Responsive Breakpoints
/// Centralized breakpoint constants for consistent responsive behavior.
class Breakpoints {
  Breakpoints._();

  /// Mobile: < 600px
  static const double mobile = 600;

  /// Tablet: 600–1024px
  static const double tablet = 1024;

  /// Desktop: > 1024px
  static const double desktop = 1024;

  /// Wide desktop: > 1440px
  static const double wideDesktop = 1440;
}
