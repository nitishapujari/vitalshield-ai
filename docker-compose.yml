version: '3.8'
services:
  backend:
    build:
      context: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/vitalshield.db:/app/vitalshield.db
    environment:
      - PYTHONUNBUFFERED=1
    restart: unless-stopped
